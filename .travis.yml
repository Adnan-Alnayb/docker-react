sudo: required 
services:
 - docker

before_install:
 - docker build -t adnanalnayb/docker-react -f Dockerfile.dev .
 

script:
 - docker run travis_wait adnanalnayb/docker-react npm run test -- --coverage